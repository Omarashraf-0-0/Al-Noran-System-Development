# ========================================
# AWS S3 CONFIGURATION FOR AL-NORAN UPLOADS
# ========================================

# AWS Region - Middle East (Bahrain)
AWS_REGION=me-south-1

# AWS S3 Bucket Name
AWS_S3_BUCKET_NAME=noran-uploads

# AWS IAM User Credentials
# Create an IAM user with S3 access permissions
# Required permissions: s3:PutObject, s3:GetObject, s3:DeleteObject, s3:ListBucket
AWS_ACCESS_KEY_ID=your_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_secret_access_key_here

# ========================================
# IMPORTANT SECURITY NOTES:
# ========================================
# 1. NEVER commit these credentials to Git
# 2. Use IAM user with minimum required permissions
# 3. Enable MFA for AWS console access
# 4. Rotate access keys regularly
# 5. Use S3 bucket policies to restrict access
# 6. Enable S3 bucket versioning for backup
# 7. Use private ACL for files (presigned URLs for access)
# ========================================

# Example IAM Policy for S3 Upload User:
# {
#   "Version": "2012-10-17",
#   "Statement": [
#     {
#       "Effect": "Allow",
#       "Action": [
#         "s3:PutObject",
#         "s3:GetObject",
#         "s3:DeleteObject",
#         "s3:ListBucket"
#       ],
#       "Resource": [
#         "arn:aws:s3:::noran-uploads/*",
#         "arn:aws:s3:::noran-uploads"
#       ]
#     }
#   ]
# }
